/**
 * Type definitions for custom hooks
 */

/**
 * Screen dimensions from useScreenDimensions hook
 */
export interface ScreenDimensions {
    width: number;
    height: number;
    isPortrait: boolean;
} 
